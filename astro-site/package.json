{
  "name": "astro-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check:a11y": "npx --yes lighthouse http://localhost:4322/websiteCode/   --only-categories=accessibility --output=html --output-path=./a11y-report.html --chrome-flags=--headless=new --quiet",
    "check:a11y:all": "npm run check:a11y && npx --yes lighthouse http://localhost:4322/websiteCode/cast --only-categories=accessibility --output=html --output-path=./a11y-report-cast.html --chrome-flags=--headless=new --quiet && npx --yes lighthouse http://localhost:4322/websiteCode/contact --only-categories=accessibility --output=html --output-path=./a11y-report-contact.html --chrome-flags=--headless=new --quiet"
  },
  "dependencies": {
    "astro": "^5.17.1"
  }
}